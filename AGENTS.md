## Git Workflow

- For this repository only, committing directly to `main` is allowed when making local project changes.
- Never use git force push.
- Keep commits atomic: commit only the files you touched and list each path explicitly.
- Use conventional-style commit messages in the format `<type>: <summary>`.
